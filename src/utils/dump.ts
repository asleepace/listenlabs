/**
 * Send output to console dump.
 * @param args
 * @returns
 */
export async function dump(...args: any[]) {
  // return fetch('https://consoledump.io/8c2ac478', { method: 'POST', body: JSON.stringify(args) }).catch(() => {})
}
